// import types

const initialState = {
  flats: null,
  current: null,
  error: null,
  filteredFlats: null
}

export default (state = initialState, action) => {
  switch(action.type) {
    default:
      return state;
  }
}